# Static Site Generator

A static site generator converts markdown files into HTML pages using templates, creating a full website from simple text content. It automatically builds and organizes pages into a ready-to-serve static folder.
